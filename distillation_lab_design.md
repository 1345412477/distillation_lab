# DistillationLab - 模型蒸馏与剪枝实验平台 (PyQt桌面版)

## 1. 项目概述

### 1.1 项目目标

构建一个 **PyQt 桌面端** 模型压缩实验平台，用户可以通过可视化界面完成以下核心工作：

- 导入教师模型和学生模型（支持 HuggingFace / ModelScope / 本地路径）
- 配置并执行知识蒸馏实验（离线数据蒸馏 + LoRA）
- 配置并执行模型剪枝实验（结构化剪枝 + LoRA 恢复）
- 通过实时数据看板监控训练过程（Loss 曲线、评估对比、参数量变化等）
- 对比蒸馏/剪枝前后的模型能力变化

### 1.2 目标用户

- 需要进行模型压缩/蒸馏研究的 AI 工程师和研究人员
- 希望将大模型部署到资源受限环境的开发者
- 学习模型压缩技术的入门者

### 1.3 核心价值

| 痛点 | 解决方案 |
|------|----------|
| 蒸馏/剪枝需要手写大量代码 | 可视化界面配置参数，一键启动实验 |
| 训练过程不透明 | 实时 Loss 曲线、GPU 状态、评估结果可视化 |
| 实验对比困难 | 多实验并行对比看板 |
| 环境配置复杂 | 统一管理模型、依赖、GPU 资源 |

### 1.4 当前实验基础

已完成的实验验证：
- **蒸馏**: Qwen3-1.7B → Qwen3-0.6B 和 Qwen3-1.7B → Qwen2.5-0.5B 成功跑通
- **剪枝**: 当前剪枝后模型性能严重下降，需要优化策略（降低剪枝比例、改进恢复微调）

---

## 2. 技术架构

### 2.1 整体架构

```
┌────────────────────────────────────────────────────────────┐
│                    PyQt6 桌面应用                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────┐    │
│  │ 模型管理  │ │ 实验配置  │ │ 实时监控  │ │ 结果对比   │    │
│  │ (QWidget) │ │ (QWidget) │ │(pyqtgraph)│ │ (QTable)   │    │
│  └──────────┘ └──────────┘ └──────────┘ └────────────┘    │
│                         │                                  │
│  ┌──────────────────────┴──────────────────────────────┐   │
│  │                 后台线程 (QThread)                     │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐             │   │
│  │  │ 蒸馏引擎  │ │ 剪枝引擎  │ │ 评估引擎  │             │   │
│  │  └──────────┘ └──────────┘ └──────────┘             │   │
│  └──────────────────────┬──────────────────────────────┘   │
│                         │                                  │
│  ┌──────────────────────┴──────────────────────────────┐   │
│  │              数据管理 (SQLite + JSON)                 │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐             │   │
│  │  │ 实验记录  │ │ 模型注册  │ │ 实验数据  │             │   │
│  │  └──────────┘ └──────────┘ └──────────┘             │   │
│  └─────────────────────────────────────────────────────┘   │
└────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈选型

| 层级 | 技术 | 选型理由 |
|------|------|----------|
| 桌面框架 | PyQt6 | 成熟稳定，信号槽机制适合实时通信 |
| 图表库 | pyqtgraph | 高性能实时图表，原生 Qt 集成 |
| 核心计算 | PyTorch + Transformers | 与现有实验脚本兼容 |
| 微调框架 | PEFT (LoRA) | 消费级显卡即可运行 |
| 数据库 | SQLite (sqlite3) | 轻量级，零配置 |
| GPU 监控 | pynvml / subprocess | 实时获取 GPU 状态 |
| 文件存储 | JSON + Safetensors | 模型和数据持久化 |

### 2.3 与 Web 方案对比优势

| 维度 | PyQt 桌面应用 | Web 方案 |
|------|-------------|----------|
| GPU 访问 | 直接调用，零延迟 | 需后端中转 |
| 实时性 | 信号槽直连，延迟极低 | WebSocket，有网络开销 |
| 部署 | 单机运行，无需服务器 | 需要维护前后端服务 |
| 资源占用 | 低，无浏览器开销 | 高（浏览器 + 后端） |
| 开发工作量 | 中等 | 较高（前后端分离） |

---

## 3. 功能模块设计

### 3.1 模型管理模块

#### 3.1.1 模型导入

支持三种导入方式：

| 方式 | 说明 |
|------|------|
| HuggingFace Hub | 输入 repo_id，自动下载（支持配置镜像） |
| ModelScope | 输入 repo_id，自动下载（国内加速） |
| 本地路径 | 指定本地模型目录路径 |
| 从实验导入 | 导入已完成的蒸馏/剪枝输出模型 |

#### 3.1.2 模型信息展示

导入后自动解析并展示：

```json
{
  "model_id": "Qwen/Qwen3-1.7B",
  "architecture": "Qwen3ForCausalLM",
  "parameters": "1.72B",
  "hidden_size": 2048,
  "num_layers": 24,
  "dtype": "bfloat16",
  "file_size": "3.5GB"
}
```

#### 3.1.3 模型能力基线测试

- 用户输入自定义测试问题集
- 模型生成回答
- 结果保存为基线快照，供后续对比

---

### 3.2 蒸馏实验模块

#### 3.2.1 实验配置界面

```
┌──────────────────────────────────────────────────────┐
│  蒸馏实验配置                                         │
├──────────────────────────────────────────────────────┤
│                                                       │
│  教师模型: [Qwen/Qwen3-1.7B    ▼]  [查看信息]          │
│  学生模型: [Qwen/Qwen2.5-0.5B  ▼]  [查看信息]          │
│                                                       │
│  ─── 数据生成配置 ───                                 │
│  生成数量:    [50    ] 条                             │
│  温度:        [0.7   ]  (0.1 ~ 2.0)                  │
│  Top-P:       [0.9   ]  (0.1 ~ 1.0)                  │
│  最大Token:   [256   ]                                │
│  启用思考:    [✓]                                     │
│                                                       │
│  ─── 训练配置 ───                                     │
│  训练方法:    ● LoRA   ○ 全参数微调                   │
│  LoRA Rank:   [16    ]  (8/16/32/64)                  │
│  LoRA Alpha:  [32    ]                                │
│  训练轮数:    [3     ]                                │
│  学习率:      [5e-5  ]                               │
│  批大小:      [2     ]                                │
│                                                       │
│              [ 开始实验 ]  [ 保存配置 ]                 │
└──────────────────────────────────────────────────────┘
```

#### 3.2.2 蒸馏流程

```
开始
 │
 ├─ 1. 数据生成阶段（后台线程，实时推送进度）
 │   ├─ 教师模型加载
 │   ├─ 种子问题输入/生成
 │   ├─ 教师逐条生成回答 → 信号推送进度
 │   └─ 保存蒸馏数据集 (JSON)
 │
 ├─ 2. 学生基线评估
 │   ├─ 加载学生模型
 │   ├─ 在测试集上评估 → 保存基线结果
 │   └─ 信号推送基线指标
 │
 ├─ 3. LoRA 蒸馏训练
 │   ├─ 包装学生模型 (LoRA)
 │   ├─ 逐 batch 训练 → 信号实时推送 Loss
 │   ├─ 每轮结束保存 checkpoint
 │   └─ 合并 LoRA 权重
 │
 ├─ 4. 蒸馏后评估
 │   ├─ 在相同测试集上评估
 │   ├─ 与基线对比 → 推送对比结果
 │   └─ 保存蒸馏后模型
 │
 └─ 结束 → 生成实验报告
```

---

### 3.3 剪枝实验模块

#### 3.3.1 实验配置界面

```
┌──────────────────────────────────────────────────────┐
│  剪枝实验配置                                         │
├──────────────────────────────────────────────────────┤
│                                                       │
│  目标模型:  [Qwen/Qwen3-1.7B    ▼]  [查看信息]         │
│                                                       │
│  ─── 剪枝配置 ───                                     │
│  剪枝策略:    ● FFN维度剪枝  ○ 注意力头剪枝  ○ 层剪枝  │
│  剪枝比例:    [20    ] %  (5% ~ 50%)                  │
│  重要性评估:  ● L1范数  ○ L2范数  ○ 基于激活值        │
│                                                       │
│  ─── 恢复微调配置 ───                                 │
│  启用恢复:    [✓]                                     │
│  恢复方法:    ● LoRA   ○ 全参数                       │
│  恢复数据:    ○ 蒸馏数据  ○ 自定义数据  ○ 基线回答     │
│  恢复轮数:    [5     ]                                │
│  恢复学习率:  [5e-5  ]                               │
│                                                       │
│              [ 开始实验 ]  [ 保存配置 ]                 │
└──────────────────────────────────────────────────────┘
```

#### 3.3.2 剪枝流程

```
开始
 │
 ├─ 1. 基线评估
 │   └─ 在测试集上评估原始模型
 │
 ├─ 2. 权重重要性分析
 │   ├─ 遍历目标层（FFN/Attention）
 │   ├─ 计算每个神经元/头的 L1 范数
 │   └─ 推送重要性分布图（前端可视化）
 │
 ├─ 3. 执行剪枝
 │   ├─ 按配置比例置零最不重要参数
 │   ├─ 统计零参数比例
 │   └─ 推送剪枝统计信息
 │
 ├─ 4. 剪枝后评估
 │   └─ 在测试集上评估（未恢复）
 │
 ├─ 5. LoRA 恢复微调（可选）
 │   ├─ LoRA 包装 + 训练
 │   ├─ 实时推送 Loss
 │   └─ 合并 LoRA 权重
 │
 ├─ 6. 恢复后评估
 │   └─ 在测试集上评估（恢复后）
 │
 └─ 结束 → 生成实验报告（三阶段对比）
```

---

### 3.4 数据可视化看板

#### 3.4.1 实时监控面板

```
┌────────────────────────────────────────────────────────────┐
│  实验监控 - 蒸馏实验 #003                  状态: ● 训练中   │
├─────────────────────────┬──────────────────────────────────┤
│                         │                                   │
│  Loss 曲线 (pyqtgraph)  │  实验信息                          │
│  ┌───────────────────┐  │  教师: Qwen3-1.7B                │
│  │  2.1 ──╮          │  │  学生: Qwen2.5-0.5B              │
│  │        ╲ 1.6       │  │  方法: LoRA (r=16)              │
│  │         ╲    ╭──1.4 │  │  数据: 50 条                    │
│  │          ╲──╯       │  │  GPU: RTX 3060 (6GB)           │
│  │                     │  │                                  │
│  └─────────────────────┘  │  当前: Epoch 3/3                │
│                            │  进度: ████████░░ 75%           │
│  GPU 使用率 (pyqtgraph)    │  速度: 2.3 it/s                 │
│  ┌───────────────────┐    │  显存: 4.2/6.0 GB              │
│  │  ████████░░  70%   │    │  已用时: 03:24                 │
│  └───────────────────┘    │  预计剩余: 01:08                 │
│                            │                                  │
│  神经元重要性分布            │                                  │
│  ┌───────────────────┐    │                                  │
│  │  ████████████░░░   │    │                                  │
│  └───────────────────┘    │                                  │
│                            │                                  │
├────────────────────────────┴──────────────────────────────────┤
│  实时日志                                                      │
│  [12:34:01] Epoch 3/3 - batch 19/25 - Loss: 1.3817          │
│  [12:33:58] Epoch 3/3 - batch 18/25 - Loss: 1.3902          │
│  [12:33:55] Epoch 3/3 - batch 17/25 - Loss: 1.4023          │
└──────────────────────────────────────────────────────────────┘
```

#### 3.4.2 实验对比看板

```
┌──────────────────────────────────────────────────────────────┐
│  实验对比                                                      │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  选择对比实验: [✓] #001 蒸馏  [✓] #002 剪枝  [✓] #003 跨架构   │
│                                                               │
│  ─── 训练 Loss 对比 (pyqtgraph叠加) ───                       │
│  ┌──────────────────────────────────────────────────────┐     │
│  │  3.0 │ #001 ─╲                                        │     │
│  │      │       ╲  #002 ···╲                             │     │
│  │  2.0 │        ╲         ···╲  #003 ───╲               │     │
│  │      │         ╲            ╲        ╲                │     │
│  │  1.0 │          ╲···         ╲······╲···              │     │
│  │      │                                               │     │
│  │  0.0 └──┬──┬──┬──┬──┬──                              │     │
│  │         1  2  3  4  5  6  (Epoch)                     │     │
│  └──────────────────────────────────────────────────────┘     │
│                                                               │
│  ─── 模型能力对比 ───                                         │
│  ┌──────────────────────────────────────────────────────┐     │
│  │  维度          │ 基线  │ #001 │ #002 │ #003           │     │
│  │  ─────────────┼──────┼──────┼──────┼──────           │     │
│  │  知识准确性    │ 0.67  │ 0.93 │ 0.20 │ 0.87           │     │
│  │  推理能力      │ 0.33  │ 0.87 │ 0.00 │ 0.80           │     │
│  │  代码生成      │ 0.67  │ 0.87 │ 0.00 │ 0.80           │     │
│  └──────────────────────────────────────────────────────┘     │
│                                                               │
│  ─── 模型规模对比 ───                                         │
│  ┌──────────────────────────────────────────────────────┐     │
│  │  参数量:  基线 0.49B → #001 0.49B  #002 1.72B  #003  │     │
│  │  显存:    基线 1.0GB → #001 1.0GB  #002 3.4GB        │     │
│  └──────────────────────────────────────────────────────┘     │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

#### 3.4.3 可视化图表清单

| 图表 | 类型 | 实现组件 | 说明 |
|------|------|---------|------|
| Loss 曲线 | 折线图 | pyqtgraph.PlotWidget | 实时更新，支持多实验叠加 |
| GPU 显存使用 | 进度条 | QProgressBar | QTimer 定时刷新 |
| GPU 利用率 | 进度条 | QProgressBar | QTimer 定时刷新 |
| 参数量变化 | 柱状图 | pyqtgraph.BarGraph | 剪枝前后/蒸馏前后对比 |
| 能力雷达图 | 雷达图 | pyqtgraph.PolarPlot | 多维度能力对比 |
| 回答质量对比 | 表格 | QTableWidget | 逐问题对比原始/处理后回答 |
| 神经元重要性分布 | 直方图 | pyqtgraph.PlotWidget | 展示各层权重分布 |

---

## 4. 数据模型设计

### 4.1 核心实体 (SQLite)

```
ModelRegistry（模型注册表）
├── id: INTEGER PRIMARY KEY
├── name: TEXT                    # 显示名称
├── source: TEXT                  # huggingface / modelscope / local
├── repo_id: TEXT                 # 仓库 ID
├── local_path: TEXT              # 本地路径
├── architecture: TEXT            # 架构名称
├── parameters: REAL              # 参数量 (B)
├── num_layers: INTEGER
├── hidden_size: INTEGER
├── dtype: TEXT
├── file_size: TEXT
├── created_at: TEXT
└── baseline_results: TEXT        # JSON 基线评估结果

ExperimentRecord（实验记录）
├── id: INTEGER PRIMARY KEY
├── name: TEXT                    # 实验名称
├── type: TEXT                    # distillation / pruning
├── status: TEXT                  # pending / running / completed / failed
├── teacher_model_id: INTEGER     # 教师模型
├── student_model_id: INTEGER     # 学生/目标模型
├── config: TEXT                  # JSON 实验配置
├── metrics: TEXT                 # JSON 训练指标
├── eval_results: TEXT            # JSON 评估结果
├── output_path: TEXT             # 输出模型路径
├── started_at: TEXT
├── completed_at: TEXT
├── error_message: TEXT
└── report_path: TEXT

DistillationData（蒸馏数据）
├── id: INTEGER PRIMARY KEY
├── experiment_id: INTEGER
├── instruction: TEXT
├── output: TEXT
├── created_at: TEXT
└── source: TEXT
```

### 4.2 信号/槽设计（替代 Web API）

```
# 后台工作线程信号
class ExperimentWorker(QThread):
    # 实时状态信号
    status_updated = pyqtSignal(str)            # 状态文本
    metric_updated = pyqtSignal(str, float, int) # 指标名, 值, step
    progress_updated = pyqtSignal(int, int)     # 当前步, 总步数
    gpu_stats_updated = pyqtSignal(float, int)  # 显存使用, 利用率
    log_updated = pyqtSignal(str)               # 日志行
    
    # 阶段性信号
    data_generated = pyqtSignal(int)            # 生成了 N 条数据
    baseline_ready = pyqtSignal(dict)           # 基线评估结果
    epoch_ended = pyqtSignal(int, dict)         # 轮次结束
    prune_stats_ready = pyqtSignal(dict)        # 剪枝统计
    importance_distribution = pyqtSignal(dict)  # 重要性分布
    
    # 结束信号
    experiment_completed = pyqtSignal(dict)     # 实验完成结果
    experiment_error = pyqtSignal(str)          # 错误信息
```

---

## 5. 应用页面结构

### 5.1 主窗口布局

```
┌──────────────────────────────────────────────────────────┐
│  DistillationLab - 模型蒸馏与剪枝实验平台       [_][□][x] │
├──────────┬───────────────────────────────────────────────┤
│          │                                                │
│  导航栏   │                                                │
│          │                                                │
│  [🏠 首页]│              主内容区 (QStackedWidget)          │
│  [📦 模型]│                                                │
│  [🔬 蒸馏]│    ┌─────────────────────────────────────┐     │
│  [✂️ 剪枝] │    │                                     │     │
│  [📊 监控] │    │   根据左侧导航切换对应页面              │     │
│  [📈 对比] │    │                                     │     │
│          │    └─────────────────────────────────────┘     │
│          │                                                │
│  GPU 状态 │                                                │
│  ──────── │                                                │
│  GPU 0    │                                                │
│  ████ 70% │                                                │
│  4.2/6GB  │                                                │
├──────────┴───────────────────────────────────────────────┤
│  状态栏: 就绪                                      v1.0.0 │
└──────────────────────────────────────────────────────────┘
```

### 5.2 页面路由（QStackedWidget 索引）

| 索引 | 页面 | 功能 |
|-----|------|------|
| 0 | 首页概览 | 实验统计、快速入口、GPU 状态 |
| 1 | 模型管理 | 模型列表、导入、详情查看 |
| 2 | 蒸馏实验 | 配置参数、启动蒸馏、查看进度 |
| 3 | 剪枝实验 | 配置参数、启动剪枝、查看进度 |
| 4 | 实时监控 | 选中的实验详情、Loss 曲线、日志 |
| 5 | 实验对比 | 多实验选择、叠加对比、能力对比 |

---

## 6. 核心引擎设计

### 6.1 蒸馏引擎（封装现有实验代码）

```python
class DistillationEngine:
    """蒸馏引擎 - 封装现有 mini_distill_experiment.py 的逻辑"""
    
    def __init__(self, config: dict, worker: 'ExperimentWorker'):
        self.config = config
        self.worker = worker  # 通过信号与UI通信
    
    def run(self):
        """执行蒸馏全流程"""
        # 1. 加载教师模型
        teacher = self.load_teacher()
        self.worker.status_updated.emit("教师模型加载完成")
        
        # 2. 生成蒸馏数据
        data = self.generate_data(teacher)
        self.worker.data_generated.emit(len(data))
        
        # 3. 加载学生 + 基线评估
        student = self.load_student()
        baseline = self.evaluate(student)
        self.worker.baseline_ready.emit(baseline)
        
        # 4. LoRA 蒸馏训练
        student = self.apply_lora(student)
        for epoch in range(epochs):
            for batch in dataloader:
                loss = self.train_step(student, batch)
                self.worker.metric_updated.emit("loss", loss, step)
            self.worker.epoch_ended.emit(epoch, {"loss": avg_loss})
        
        # 5. 合并 + 评估 + 保存
        student = self.merge_lora(student)
        results = self.evaluate(student)
        self.worker.experiment_completed.emit(results)
```

### 6.2 剪枝引擎（封装现有实验代码）

```python
class PruningEngine:
    """剪枝引擎 - 基于权重重要性分析"""
    
    def run(self):
        # 1. 加载模型 + 基线评估
        model = self.load_model()
        baseline = self.evaluate(model)
        
        # 2. 重要性分析 + 可视化
        importance = self.compute_importance(model)
        self.worker.importance_distribution.emit(importance)
        
        # 3. 执行剪枝
        model = self.prune(model, importance)
        stats = self.compute_stats(model)
        self.worker.prune_stats_ready.emit(stats)
        
        # 4. 剪枝后评估
        pruned_eval = self.evaluate(model)
        
        # 5. LoRA 恢复微调（增强版: 更多轮数 + 更细粒度调参）
        if self.config.enable_recovery:
            model = self.recovery_finetune(model)
        
        # 6. 恢复后评估 + 保存
        final_eval = self.evaluate(model)
        self.worker.experiment_completed.emit(final_eval)
```

---

## 7. 项目目录结构

```
distillation-lab/
├── app/                          # PyQt 应用主体
│   ├── __init__.py
│   ├── main.py                   # 应用入口
│   ├── main_window.py            # 主窗口
│   ├── pages/                    # 功能页面
│   │   ├── __init__.py
│   │   ├── dashboard.py          # 首页概览
│   │   ├── model_manager.py      # 模型管理
│   │   ├── distill_page.py       # 蒸馏实验
│   │   ├── prune_page.py         # 剪枝实验
│   │   ├── monitor_page.py       # 实时监控
│   │   └── compare_page.py       # 实验对比
│   ├── components/               # 可复用组件
│   │   ├── __init__.py
│   │   ├── loss_chart.py         # Loss 曲线图
│   │   ├── radar_chart.py        # 雷达图
│   │   ├── gpu_gauge.py          # GPU 仪表盘
│   │   └── import_dialog.py      # 导入模型对话框
│   ├── engines/                  # 核心引擎
│   │   ├── __init__.py
│   │   ├── distillation.py       # 蒸馏引擎
│   │   ├── pruning.py            # 剪枝引擎
│   │   └── evaluation.py         # 评估引擎
│   ├── workers/                  # 后台线程
│   │   ├── __init__.py
│   │   └── train_worker.py       # 训练工作线程
│   ├── models/                   # 数据管理
│   │   ├── __init__.py
│   │   ├── db_manager.py         # 数据库管理
│   │   └── model_manager.py      # 模型注册管理
│   └── utils/                    # 工具函数
│       ├── __init__.py
│       ├── config.py             # 全局配置
│       └── gpu_monitor.py        # GPU 监控
├── data/                         # 运行时数据
│   ├── models/                   # 下载的模型缓存
│   ├── experiments/              # 实验输出
│   └── db/
│       └── distillation_lab.db   # SQLite 数据库
├── mini_experiment_output/       # 已有实验输出
├── requirements.txt              # 依赖管理
└── distillation_lab_design.md    # 本设计文档
```

---

## 8. 开发计划

### Phase 1: 基础框架（当前）

| 任务 | 说明 | 状态 |
|------|------|------|
| 应用脚手架 | 主窗口 + 导航 + 页面切换 | ✅ |
| 数据库初始化 | SQLite 建表 + CRUD | ✅ |
| 模型导入 | 支持 ModelScope/HuggingFace/本地路径 | ✅ |
| 模型列表页 | 展示已导入模型信息 | ✅ |

### Phase 2: 蒸馏功能

| 任务 | 说明 | 状态 |
|------|------|------|
| 蒸馏配置页 | 表单配置教师/学生/训练参数 | ✅ |
| 蒸馏引擎 | 数据生成 + LoRA 训练 + 评估 | ✅ (封装现有代码) |
| 实时监控 | 信号推送 Loss + GPU 状态 | ✅ |
| 实验结果页 | 蒸馏前后对比展示 | ✅ |

### Phase 3: 剪枝功能（优化版）

| 任务 | 说明 | 状态 |
|------|------|------|
| 剪枝配置页 | 表单配置剪枝策略/比例/恢复参数 | ✅ |
| 剪枝引擎 | 重要性分析 + 剪枝 + LoRA 恢复（增强恢复） | ✅ |
| 重要性可视化 | 神经元权重分布直方图 | ✅ |
| 三阶段对比 | 剪枝前/剪枝后/恢复后对比 | ✅ |

### Phase 4: 看板与对比

| 任务 | 说明 | 状态 |
|------|------|------|
| 概览看板 | 首页实验概览 + GPU 状态 | ✅ |
| 实验对比 | 多实验 Loss 叠加 + 能力雷达图 | ✅ |
| 实验报告 | 自动生成报告 | ✅ |
| 优化打磨 | UI 细节、错误处理 | ✅ |

---

## 9. 关键设计决策

### 9.1 为什么用离线蒸馏而非在线蒸馏？

| 维度 | 离线蒸馏 | 在线蒸馏 |
|------|----------|----------|
| 架构要求 | 教师学生可不同架构 | 需要兼容的 logits 维度 |
| 显存需求 | 先后加载，峰值低 | 同时加载，显存翻倍 |
| 实现复杂度 | 低 | 高（需对齐 tokenization） |
| 适用场景 | 通用 | 特定架构对 |

### 9.2 为什么用 LoRA 而非全参数微调？

基于实际实验的结论：
- 全参数微调在 6GB 显存上出现 NaN loss 和 OOM
- LoRA 训练完全稳定，loss 持续下降
- LoRA 显存需求低，消费级显卡即可运行

### 9.3 剪枝优化策略

针对当前剪枝后性能严重下降问题，采用的优化方案：
1. **降低剪枝比例**: 从 50% 降至 20%，渐进式尝试
2. **增加恢复轮数**: 从 3 轮增至 5 轮，提高恢复质量
3. **更好的恢复数据**: 使用蒸馏数据而非基线回答
4. **重要性可视化**: 让用户直观看到权重分布，选择合理的剪枝比例
5. **渐进式剪枝**: 支持多轮渐进剪枝，每轮剪枝后恢复再继续剪枝

---

## 10. 风险与应对

| 风险 | 影响 | 应对方案 |
|------|------|----------|
| 大模型下载失败 | 实验无法启动 | 支持断点续传、多镜像源切换 |
| GPU 显存不足 | 训练 OOM | 自动检测显存，推荐合适配置 |
| 训练 NaN loss | 实验失败 | 自动检测 + 告警 + 建议降学习率 |
| PyQt 线程阻塞 | UI 卡死 | 所有耗时操作放 QThread |
| 实验中断 | 进度丢失 | 自动保存 checkpoint，支持断点续训 |
| 剪枝性能失衡 | 模型能力丧失 | 降低剪枝比例、增强恢复微调、渐进式剪枝 |
